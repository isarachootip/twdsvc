# STEP-23 — S2 งานซ่อมสต็อกสาขา

← [STEP-22](STEP-22_tradein.md) · [สารบัญ](README.md) · ถัดไป → [STEP-24 — รายงานจ่ายเงิน VD](STEP-24_payout.md)

**เป้าหมาย:** สาขาส่งสินค้าสต็อกไปซ่อมได้โดยไม่มีขั้นตอนการเงิน
**เอกสารอ้างอิง:** docs/07_screens.md §S2, docs/04_workflow_state_machine.md §5, docs/prototypes/s2.html
**ต้องผ่านก่อน:** tag `step-22`

---

## ① ก่อนเริ่ม
```bash
git status            # ต้องไม่มีไฟล์ค้าง (clean)
docker compose up -d  # ให้ฐานข้อมูลทำงาน
```

## ② คัดลอก Prompt นี้ไปวางใน AI Agent
````text
คุณกำลังพัฒนาระบบ SVCM ตามเอกสารในโฟลเดอร์ docs/
ก่อนเริ่ม ให้อ่าน AGENTS.md และเอกสารอ้างอิงของ step นี้: docs/07_screens.md §S2, docs/04_workflow_state_machine.md §5, docs/prototypes/s2.html

## งานของ Step 23 — S2 งานซ่อมสต็อกสาขา
เป้าหมาย: สาขาส่งสินค้าสต็อกไปซ่อมได้โดยไม่มีขั้นตอนการเงิน

ขอบเขตงาน:
1. หน้า `/s2`: เลือกศูนย์ซ่อม (combobox), ผู้รับเรื่อง, บรรทัด SKU หลายแถว, วิธีจัดส่ง DC/VD (disable ตัวที่ศูนย์ไม่รองรับ), ปุ่มเปิดงาน
2. สติ๊กเกอร์ 2×2 นิ้ว บน A4 4 คอลัมน์ 1 ดวงต่อ 1 ชิ้น (`/documents/stock-jobs/:id/stickers`) + print modal ตาม s2.html
3. ตารางงานสต็อก + ค้นหา + พิมพ์ซ้ำ + ปิดงานเมื่อพร้อมรับ
4. ตรวจว่า GR/DC/VD หน้าจอเดิมรองรับงาน STOCK (ไม่แสดงลูกค้า/ยอดเงิน, VD มีปุ่มเริ่มซ่อมแทนเสนอราคา)

กติกา:
- ทำเฉพาะขอบเขตของ step นี้ ห้ามทำงานของ step ถัดไปล่วงหน้า
- ถ้าเอกสารไม่ชัดหรือขัดกัน ให้เลือกตามลำดับความจริงใน AGENTS.md และจดคำถามไว้ใน docs/OPEN_QUESTIONS.md
- เมื่อเสร็จ ให้รัน `pnpm lint && pnpm typecheck && pnpm test` จนผ่าน
- ห้าม git commit (ผมจะตรวจแล้ว commit เอง)

เมื่อเสร็จให้สรุป: (1) ไฟล์ที่สร้าง/แก้ (2) วิธีทดสอบด้วยมือทีละข้อ (3) สิ่งที่ยังไม่ได้ทำหรือคำถามค้าง
````

## ③ ตรวจผลด้วยตัวเอง (ติ๊กให้ครบก่อนไป step ถัดไป)
| # | ทำอะไร | ต้องเห็นอะไร | ผ่าน |
|---|--------|--------------|:----:|
| 1 | เปิดงาน 2 SKU (จำนวน 2 และ 1) | สติ๊กเกอร์ 3 ดวง มี (1/2) (2/2) | ☐ |
| 2 | เดินงานผ่าน GR → VD | VD ไม่มีปุ่มเสนอราคา มีปุ่มเริ่มซ่อม | ☐ |
| 3 | งานกลับถึงสาขา S2 กดปิดงาน | CLOSED_REPAIRED และไม่มี JobCharge/Payment | ☐ |
| 4 | `pnpm lint && pnpm typecheck && pnpm test` | ผ่านทั้งหมด | ☐ |

## ④ บันทึกงาน
```bash
git add -A
git commit -m "feat(s2): stock repair jobs and 2x2 stickers"
git tag step-23
```

## ถ้าไม่ผ่าน
- ข้อไหนไม่ผ่าน → ใช้ **Prompt แก้ปัญหา** ใน [README](README.md#prompt-แก้ปัญหา) แนบ error/ภาพหน้าจอ
- อยากเริ่ม step นี้ใหม่ทั้งหมด → `git reset --hard step-22 && git clean -fd`
