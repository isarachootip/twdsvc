# STEP-17 — หน้าจอ VD: งานรอรับ + จำลอง 3PL

← [STEP-16](STEP-16_dc.md) · [สารบัญ](README.md) · ถัดไป → [STEP-18 — VD ประเมิน + เสนอราคา](STEP-18_vd-quote.md)

**เป้าหมาย:** VD รับงานเข้าศูนย์ได้ทั้ง 3 ช่องทาง
**เอกสารอ้างอิง:** docs/07_screens.md §VD (แท็บงานรอรับ), docs/prototypes/vd.html
**ต้องผ่านก่อน:** tag `step-16`

---

## ① ก่อนเริ่ม
```bash
git status            # ต้องไม่มีไฟล์ค้าง (clean)
docker compose up -d  # ให้ฐานข้อมูลทำงาน
```

## ② คัดลอก Prompt นี้ไปวางใน AI Agent
````text
คุณกำลังพัฒนาระบบ SVCM ตามเอกสารในโฟลเดอร์ docs/
ก่อนเริ่ม ให้อ่าน AGENTS.md และเอกสารอ้างอิงของ step นี้: docs/07_screens.md §VD (แท็บงานรอรับ), docs/prototypes/vd.html

## งานของ Step 17 — หน้าจอ VD: งานรอรับ + จำลอง 3PL
เป้าหมาย: VD รับงานเข้าศูนย์ได้ทั้ง 3 ช่องทาง

ขอบเขตงาน:
1. หน้า `/vd` โครง 5 แท็บด้วย QueuePage (แท็บอื่นขึ้นตารางอย่างเดียวก่อน) + banner + KPI + date range + Export
2. แท็บงานรอรับ: filter เข้ารับที่ DC / เข้ารับที่สาขา / รอ 3PL มาส่ง, action cell ตามสถานะใน 07 §VD (Print/Gen Link, ถ่ายภาพยืนยันรับ, ส่งมอบช่าง), ช่องสแกนแล้วกด Enter
3. งาน 3PL: badge รอ 3PL + ปุ่ม "จำลอง: API 3PL แจ้งส่งสำเร็จ" (แสดงเฉพาะ non-production) เรียก webhook mock

กติกา:
- ทำเฉพาะขอบเขตของ step นี้ ห้ามทำงานของ step ถัดไปล่วงหน้า
- ถ้าเอกสารไม่ชัดหรือขัดกัน ให้เลือกตามลำดับความจริงใน AGENTS.md และจดคำถามไว้ใน docs/OPEN_QUESTIONS.md
- เมื่อเสร็จ ให้รัน `pnpm lint && pnpm typecheck && pnpm test` จนผ่าน
- ห้าม git commit (ผมจะตรวจแล้ว commit เอง)

เมื่อเสร็จให้สรุป: (1) ไฟล์ที่สร้าง/แก้ (2) วิธีทดสอบด้วยมือทีละข้อ (3) สิ่งที่ยังไม่ได้ทำหรือคำถามค้าง
````

## ③ ตรวจผลด้วยตัวเอง (ติ๊กให้ครบก่อนไป step ถัดไป)
| # | ทำอะไร | ต้องเห็นอะไร | ผ่าน |
|---|--------|--------------|:----:|
| 1 | login vd.0088 แท็บงานรอรับ | เห็นงานแยกประเภทรับเข้าได้ถูก | ☐ |
| 2 | งาน DSD: Gen Link → ถ่ายภาพ → ยืนยันรับ → ส่งมอบช่าง | งานไปแท็บประเมิน/เสนอราคา | ☐ |
| 3 | งาน 3PL กดจำลอง webhook | งานไปแท็บประเมิน/เสนอราคาอัตโนมัติ | ☐ |
| 4 | login vd อื่น (สร้างเพิ่มใน admin) | ไม่เห็นงานของ VD-0088 | ☐ |
| 5 | `pnpm lint && pnpm typecheck && pnpm test` | ผ่านทั้งหมด | ☐ |

## ④ บันทึกงาน
```bash
git add -A
git commit -m "feat(vd): receive queue and 3PL simulation"
git tag step-17
```

## ถ้าไม่ผ่าน
- ข้อไหนไม่ผ่าน → ใช้ **Prompt แก้ปัญหา** ใน [README](README.md#prompt-แก้ปัญหา) แนบ error/ภาพหน้าจอ
- อยากเริ่ม step นี้ใหม่ทั้งหมด → `git reset --hard step-16 && git clean -fd`
