# STEP-20 — ขาซ่อมและส่งคืน: VD → DC → GR

← [STEP-19](STEP-19_public-pages.md) · [สารบัญ](README.md) · ถัดไป → [STEP-21 — CS คิวรับคืน + ปิดงาน + แบบประเมิน](STEP-21_cs-close.md)

**เป้าหมาย:** สินค้ากลับถึงสาขาได้ครบทุกช่องทาง
**เอกสารอ้างอิง:** docs/07_screens.md §VD (กำลังซ่อม, Pack และส่งคืน), §DC (รับคืน, ส่งคืนสาขา), §GR (รับคืน, ส่งมอบ CS), docs/04_workflow_state_machine.md §4
**ต้องผ่านก่อน:** tag `step-19`

---

## ① ก่อนเริ่ม
```bash
git status            # ต้องไม่มีไฟล์ค้าง (clean)
docker compose up -d  # ให้ฐานข้อมูลทำงาน
```

## ② คัดลอก Prompt นี้ไปวางใน AI Agent
````text
คุณกำลังพัฒนาระบบ SVCM ตามเอกสารในโฟลเดอร์ docs/
ก่อนเริ่ม ให้อ่าน AGENTS.md และเอกสารอ้างอิงของ step นี้: docs/07_screens.md §VD (กำลังซ่อม, Pack และส่งคืน), §DC (รับคืน, ส่งคืนสาขา), §GR (รับคืน, ส่งมอบ CS), docs/04_workflow_state_machine.md §4

## งานของ Step 20 — ขาซ่อมและส่งคืน: VD → DC → GR
เป้าหมาย: สินค้ากลับถึงสาขาได้ครบทุกช่องทาง

ขอบเขตงาน:
1. VD แท็บกำลังซ่อม: ระยะเวลาซ่อม, SLA, toggle รออะไหล่/ซ่อมต่อ (pause/resume), ปุ่มซ่อมเสร็จ
2. VD แท็บ Pack และส่งคืน: DC/DSD ถ่ายภาพ + Pack พิมพ์ใบปะหน้าขาคืน; 3PL ปุ่ม Confirm Auto Book
3. DC แท็บรับคืนจาก VD + ส่งคืนกลับสาขา (Print/Link + ยืนยันจัดส่ง) ใช้งานได้จริง
4. GR แท็บรับคืน + ส่งมอบ CS ใช้งานได้จริง; ส่งมอบ CS แล้วส่ง LON แจ้งพร้อมรับ (มีลิงก์ `/pay` ถ้ายอดค้าง)
5. 3PL ขาคืน: ปุ่มจำลอง webhook delivered ที่แท็บรับคืนของ GR (non-production)

กติกา:
- ทำเฉพาะขอบเขตของ step นี้ ห้ามทำงานของ step ถัดไปล่วงหน้า
- ถ้าเอกสารไม่ชัดหรือขัดกัน ให้เลือกตามลำดับความจริงใน AGENTS.md และจดคำถามไว้ใน docs/OPEN_QUESTIONS.md
- เมื่อเสร็จ ให้รัน `pnpm lint && pnpm typecheck && pnpm test` จนผ่าน
- ห้าม git commit (ผมจะตรวจแล้ว commit เอง)

เมื่อเสร็จให้สรุป: (1) ไฟล์ที่สร้าง/แก้ (2) วิธีทดสอบด้วยมือทีละข้อ (3) สิ่งที่ยังไม่ได้ทำหรือคำถามค้าง
````

## ③ ตรวจผลด้วยตัวเอง (ติ๊กให้ครบก่อนไป step ถัดไป)
| # | ทำอะไร | ต้องเห็นอะไร | ผ่าน |
|---|--------|--------------|:----:|
| 1 | เดินงาน channel DC จากกำลังซ่อมจนถึงรอส่งมอบ CS ผ่านหน้าจอ | ทุกขั้นผ่าน ไม่ต้องแก้ DB | ☐ |
| 2 | กด รออะไหล่ ค้างไว้แล้วดู detail | SLA ขั้นซ่อมหยุดนับ | ☐ |
| 3 | งาน 3PL ขาคืน | มี trackingNo และใบปะหน้า | ☐ |
| 4 | GR ส่งมอบ CS | NotificationLog มีข้อความพร้อมรับสินค้า | ☐ |
| 5 | `pnpm lint && pnpm typecheck && pnpm test` | ผ่านทั้งหมด | ☐ |

## ④ บันทึกงาน
```bash
git add -A
git commit -m "feat(flow): repair, return packing, DC inbound, GR return screens"
git tag step-20
```

## ถ้าไม่ผ่าน
- ข้อไหนไม่ผ่าน → ใช้ **Prompt แก้ปัญหา** ใน [README](README.md#prompt-แก้ปัญหา) แนบ error/ภาพหน้าจอ
- อยากเริ่ม step นี้ใหม่ทั้งหมด → `git reset --hard step-19 && git clean -fd`
