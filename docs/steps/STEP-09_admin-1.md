# STEP-09 — หน้าตั้งค่าระบบหลังบ้าน ส่วนที่ 1

← [STEP-08](STEP-08_integrations.md) · [สารบัญ](README.md) · ถัดไป → [STEP-10 — หน้าตั้งค่าระบบหลังบ้าน ส่วนที่ 2](STEP-10_admin-2.md)

**เป้าหมาย:** Admin จัดการข้อมูลพื้นฐานได้เอง
**เอกสารอ้างอิง:** docs/07_screens.md §Admin (ข้อ 2, 3, 7, 8, 11), docs/06_api.md §2, docs/prototypes/admin.html
**ต้องผ่านก่อน:** tag `step-08`

---

## ① ก่อนเริ่ม
```bash
git status            # ต้องไม่มีไฟล์ค้าง (clean)
docker compose up -d  # ให้ฐานข้อมูลทำงาน
```

## ② คัดลอก Prompt นี้ไปวางใน AI Agent
````text
คุณกำลังพัฒนาระบบ SVCM ตามเอกสารในโฟลเดอร์ docs/
ก่อนเริ่ม ให้อ่าน AGENTS.md และเอกสารอ้างอิงของ step นี้: docs/07_screens.md §Admin (ข้อ 2, 3, 7, 8, 11), docs/06_api.md §2, docs/prototypes/admin.html

## งานของ Step 09 — หน้าตั้งค่าระบบหลังบ้าน ส่วนที่ 1
เป้าหมาย: Admin จัดการข้อมูลพื้นฐานได้เอง

ขอบเขตงาน:
1. Layout `/admin/[section]` sidebar ย่อยมีเลขลำดับตาม admin.html (ครบ 12 หมวด, หมวดที่ยังไม่ทำให้ขึ้น "กำลังพัฒนา")
2. หมวด 2 ค่าดำเนินการ/ค่าขนส่ง (+ เพิ่มประเภทสินค้า), หมวด 3 สาขาไทวัสดุ + ผู้จัดการเขต, หมวด 7 SKU ค่าซ่อม, หมวด 8 รอบจ่ายเงิน Vendor, หมวด 11 ตั้งค่าทั่วไป
3. API ของหมวดดังกล่าวตาม 06 §2 + validation 05 §9 + บันทึก `AuditLog` ทุกการแก้ไข
4. ปุ่มบันทึกแยกต่อการ์ด + toast "บันทึกแล้ว ✓"; ลบแถวที่ถูกอ้างอิงอยู่ → ถาม confirm และทำ archive แทนลบ

กติกา:
- ทำเฉพาะขอบเขตของ step นี้ ห้ามทำงานของ step ถัดไปล่วงหน้า
- ถ้าเอกสารไม่ชัดหรือขัดกัน ให้เลือกตามลำดับความจริงใน AGENTS.md และจดคำถามไว้ใน docs/OPEN_QUESTIONS.md
- เมื่อเสร็จ ให้รัน `pnpm lint && pnpm typecheck && pnpm test` จนผ่าน
- ห้าม git commit (ผมจะตรวจแล้ว commit เอง)

เมื่อเสร็จให้สรุป: (1) ไฟล์ที่สร้าง/แก้ (2) วิธีทดสอบด้วยมือทีละข้อ (3) สิ่งที่ยังไม่ได้ทำหรือคำถามค้าง
````

## ③ ตรวจผลด้วยตัวเอง (ติ๊กให้ครบก่อนไป step ถัดไป)
| # | ทำอะไร | ต้องเห็นอะไร | ผ่าน |
|---|--------|--------------|:----:|
| 1 | แก้ค่าดำเนินการสินค้าขนาดใหญ่เป็น 350 แล้วรีเฟรช | ค่ายังเป็น 350; มีแถวใหม่ใน AuditLog | ☐ |
| 2 | เพิ่มสาขาใหม่ ประเภท DC | ช่องผู้จัดการเขตถูก disable | ☐ |
| 3 | login gr.bangna เรียก API `/fee-rates` แบบ PUT | 403 | ☐ |
| 4 | `pnpm lint && pnpm typecheck && pnpm test` | ผ่านทั้งหมด | ☐ |

## ④ บันทึกงาน
```bash
git add -A
git commit -m "feat(admin): fees, sites, skus, payout config, settings"
git tag step-09
```

## ถ้าไม่ผ่าน
- ข้อไหนไม่ผ่าน → ใช้ **Prompt แก้ปัญหา** ใน [README](README.md#prompt-แก้ปัญหา) แนบ error/ภาพหน้าจอ
- อยากเริ่ม step นี้ใหม่ทั้งหมด → `git reset --hard step-08 && git clean -fd`
